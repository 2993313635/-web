<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boshuo.sjzgl.mapper.GameGuideMapper">

    <resultMap id="BaseResultMap" type="GameGuide">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="categoryId" column="category_id"/>
        <result property="coverImage" column="cover_image"/>
        <result property="videoUrl" column="video_url"/>
        <result property="attachmentUrl" column="attachment_url"/>
        <result property="viewCount" column="view_count"/>
        <result property="likeCount" column="like_count"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="Base_Column_List">
        `id`,`title`, `content`, `category_id`, `cover_image`, `video_url`,
        `attachment_url`,
        `view_count`, `like_count`, `status`, `created_at`, `updated_at`
    </sql>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM game_guides
        WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM game_guides
        ORDER BY created_at DESC
    </select>

    <select id="findByCategoryId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM game_guides
        WHERE category_id = #{categoryId}
        ORDER BY created_at DESC
    </select>

    <select id="findByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM game_guides
        WHERE status = #{status}
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="GameGuide" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO game_guides (
            id,
            title, content, category_id, cover_image, video_url,
            attachment_url,
            view_count, like_count, status, created_at, updated_at
        ) VALUES (
                    null,
                     #{title}, #{content}, #{categoryId}, #{coverImage}, #{videoUrl},
                     #{attachmentUrl},
                     #{viewCount}, #{likeCount}, #{status}, #{createdAt}, #{updatedAt}
                 )
    </insert>

    <update id="update" parameterType="GameGuide">
        UPDATE game_guides
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="videoUrl != null">video_url = #{videoUrl},</if>
            <if test="attachmentUrl != null">attachment_url = #{attachmentUrl},</if>
            <if test="viewCount != null">view_count = #{viewCount},</if>
            <if test="likeCount != null">like_count = #{likeCount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM game_guides WHERE id = #{id}
    </delete>

    <update id="incrementViewCount">
        UPDATE game_guides
        SET view_count = view_count + 1,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="incrementLikeCount">
        UPDATE game_guides
        SET like_count = like_count + 1,
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
